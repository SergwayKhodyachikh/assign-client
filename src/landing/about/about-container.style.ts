import { Container } from '@material-ui/core';
import styled from 'styled-components';

export const AboutContainer = styled(Container).attrs({ maxWidth: 'lg' })`
  display: flex;
  justify-content: center;
  align-items: center;
`;
